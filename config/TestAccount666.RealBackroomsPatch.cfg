## Settings file was created by plugin RealBackroomsPatch v1.1.0
## Plugin GUID: TestAccount666.RealBackroomsPatch

[Chances]

## The chance that a person will get teleported into the backrooms with lethal damage (Requires "Prevent death teleport" set to "false")
# Setting type: Int32
# Default value: 1
Teleport lethal damage chance = 20

## The chance that a person will get teleported into the backrooms with non-lethal damage (Requires "Prevent damage teleport" set to "false")
# Setting type: Int32
# Default value: 3
Teleport non-lethal damage chance = 1

[Extra]

## If true, will teleport annoying people (e.g. People using horns)
# Setting type: Boolean
# Default value: false
Teleport annoying people = true

## The chance that annoying people can get teleported
# Setting type: Int32
# Default value: 10
Teleport annoying people chance = 15

[Light Flickering]

## How much to smooth out the randomness; lower values = sparks, higher = lantern
# Setting type: Int32
# Default value: 25
# Acceptable value range: From 1 to 50
Smoothing = 25

## Minimum random light intensity
# Setting type: Single
# Default value: 0.2
# Acceptable value range: From 0 to 2
Min intensity = 0.2

## Maximum random light intensity
# Setting type: Single
# Default value: 1.5
# Acceptable value range: From 0 to 2
Max intensity = 1.5

[Music]

## If true, plays music in the backrooms
# Setting type: Boolean
# Default value: true
Play Music = true

[Teleport]

## If true, will prevent the backrooms mod from teleporting players that received damage
# Setting type: Boolean
# Default value: true
Prevent damage teleport = false

## If true, will prevent the backrooms mod from teleporting players that received deadly damage
# Setting type: Boolean
# Default value: false
Prevent death teleport = false

## If true, will prevent the backrooms mod from teleporting players that received negative damage (Aka healing)
# Setting type: Boolean
# Default value: false
Prevent negative damage teleport = false

## When do you want items to be dropped when teleported into the backrooms?
# Setting type: DropHeldItemsMode
# Default value: DropNothing
# Acceptable values: DropNothing, DropAll, DropAllOnDeath, DropAllOnAnnoying, DropAllOnAnnoyingAndDeath
Drop held items on teleport = DropAll

## A list of enemies that will not allow you to spawn in the backrooms, if attacked by them (Needs internal name. Example: Flowerman, Jester, ...)
# Setting type: String
# Default value: Flowerman, MaskedPlayer
Prevent backrooms when attacked by enemies = Flowerman

